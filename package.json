{
  "name": "awesome-agentic-devsecops",
  "private": true,
  "version": "0.1.0",
  "description": "A curated atlas of AI agents that secure the software lifecycle — from threat modeling to SOC response.",
  "scripts": {
    "readme": "node scripts/generate-readme.mjs",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build"
  }
}
